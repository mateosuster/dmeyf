190*.8
18*5322.06
financiado ? 18*5322.06
financiado = 18*5322.06
financiado - 72579
240000/577
t0 = Sys.time()
t1 = Sys.time()
delta = t1- t0
delta
require("data.table")
fwrite(x = delta, file = "prueba.csv")
fwrite(x = delta, file = "prueba.txt")
data.table(delta)
fwrite(x = data.table(delta), file = "prueba.txt")
256/128
require("primes")  #para generar semillas
#me genero un vector de semilla buscando numeros primos al azar
primos  <- generate_primes(min=100000, max=1000000)  #genero TODOS los numeros primos entre 100k y 1M
ksemillas  <- sample(primos)[ 1:CANTIDAD_SEMILLAS ]   #me quedo con CANTIDAD_SEMILLAS primos al azar
CANTIDAD_SEMILLAS  <- 500
ksemillas  <- sample(primos)[ 1:CANTIDAD_SEMILLAS ]   #me quedo con CANTIDAD_SEMILLAS primos al azar
ksemillas
ksemillas  <- c( 999983, ksemillas )
ksemillas
